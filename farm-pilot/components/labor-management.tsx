'use client';

// Labor management UI is temporarily disabled; keep a minimal stub component

export function LaborManagement() {
  return (
    <div className="py-12">
      <div className="max-w-2xl mx-auto text-center">
        <h2 className="text-2xl font-semibold">Labor Management (Temporarily Disabled)</h2>
        <p className="mt-2 text-sm text-muted-foreground">
          The labor management section is disabled for now. Re-enable when backend APIs are ready.
        </p>
      </div>
    </div>
  );
}
